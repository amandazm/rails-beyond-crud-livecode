<%= simple_form_for [@stripclub, @review] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :content %>
    <%= f.input :rating, collection: [0,1,2,3] %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
